import 'package:flutter/material.dart';

class ScanQRScreena extends StatelessWidget {
  const ScanQRScreena({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
